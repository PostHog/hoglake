# Pinned dependency clones (see scripts/fetch-deps.sh); become git
# submodules when this extension moves to its own repo.
/duckdb/
/extension-ci-tools/
/build/
/testext
/duckdb_unittest_tempdir/
.DS_Store
/.cache/
test/fixtures/live-env.sh

# Working documents, per the repo convention that in-flight worklists
# and bug-hunt writeups stay local-only (AGENT.md "Doc index"): the
# milestone log and the adversarial-review rounds. Their conclusions
# live in DESIGN.md/PARITY.md/README.md and in PR bodies.
/PROGRESS.md
/REVIEW-FINDINGS*.md
