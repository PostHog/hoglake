# Adversarial review findings — round 3 (2026-09-12)

13 confirmed of 15 raw; 1 refuted.
One verifier died on a transient error; its finding is listed under Unverified below and must be assessed.

## R3-1. [HIGH] (catalog) src/common/hoglake_types.cpp:54

**Claim:** ToDuckDBType trusts wire decimal type_params: NumericCast<uint8_t>(precision) and LogicalType::DECIMAL throw InternalException for precision > 255 / width > 38, and DuckDB escalates InternalException to whole-instance invalidation, so one in-contract server table poisons every DuckDB client that attaches the catalog.

**Failure scenario:** Live-reproduced on the dev stack: POST /tables with {"type":"decimal","type_params":{"precision":100,"scale":2}} succeeds (hoglake.yaml type_params is additionalProperties:true and no service validates it; precision 300 also accepted); the extension's SELECT * FROM that table throws 'INTERNAL Error: Decimal has a width of 100...' and SHOW ALL TABLES on the catalog then fails with 'FATAL Error: database has been invalidated ... must be restarted' — every listing or lookup in the attached catalog kills the entire DuckDB instance (all catalogs, in-memory state) until the table is repaired via another client.

**Verifier:** Confirmed end-to-end in source with no refuting guard on any layer. Server: hoglake.yaml type_params is additionalProperties:true, and CatalogService.createTable (CatalogService.kt:180-187) validates only identifiers/duplicates — precision 100 or 300 is persisted in-contract. Client: hoglake_api_client.cpp:110 parses precision as unbounded int32; hoglake_types.cpp:49-54 guards only precision<=0, then for 39-255 LogicalType::DECIMAL's constructor eagerly resolves the physical type (duckdb/src/common/types.cpp:44-48) and throws InternalException at types.cpp:117, and for >255 NumericCast<uint8_t> throws InternalException (numeric_utils.hpp:50). Reachability through listing is worse than a per-table error: hoglake_schema_entry.cpp:183-190's skip-unrepresentable-tables wrapper catches only CatalogException, so both direct lookup and LoadAllTablesInternal/Scan (SHOW ALL TABLES, any catalog scan) leak the InternalException. DuckDB escalates it: client_context.cpp:368-370 invalidates the whole database instance on ExceptionType::INTERNAL, after which every query on any attached catalog fails FATAL 'database has been invalidated' until restart — matching the claimed repro. Not a documented divergence (DESIGN.md has no decimal-width caveat), untouched by both fix waves, and the neighboring case-colliding-column guard shows the intended pattern (targeted CatalogException) that decimal width lacks. Severity high stands: one in-contract table poisons the entire instance and cannot be avoided by skipping the table, since listing alone triggers it.

## R3-2. [MEDIUM] (wire) src/common/hoglake_types.cpp:134

**Claim:** CanonicalInstant parses via Value::DefaultCastAs(TIMESTAMP_TZ), which builds an empty CastFunctionSet with no client context (vendored value.cpp:2159-2163), so the session TimeZone can never influence naive inputs — yet hoglake_types.hpp:29-30, hoglake_storage.cpp:31-34, and the R2-1 fix disposition all document 'naive inputs follow the session TimeZone', making naive AT/SNAPSHOT_TIME inputs silently mean a different instant than the same literal means everywhere else in an ICU-bearing session.

**Failure scenario:** On a DuckDB host with ICU (standard distributions; this repo's test build omits it, so the suite cannot see the divergence): SET TimeZone='Europe/Berlin'; SELECT count(*) FROM lake.ns1.points AT (TIMESTAMP => '2026-09-12 10:24:58.781728') sends 10:24:58Z — but every query-level cast in that session ('2026-09-12 10:24:58.781728'::TIMESTAMPTZ) means 08:24:58Z (the fixture's T1), so the user silently reads the head snapshot (5 rows) instead of the historical 3, with no error; same for ATTACH (SNAPSHOT_TIME '...'). The in-tree documented semantics are false in code (verified statically: ICU registers its cast in DBConfig's CastFunctionSet, which DefaultCastAs never consults), so this is a silent wrong-snapshot divergence, not a documented design choice.

**Verifier:** Confirmed by full static path-sweep. Mechanism: hoglake_types.cpp:134 uses Value::DefaultCastAs(TIMESTAMP_TZ), which (vendored value.cpp:2159-2163) builds a fresh empty CastFunctionSet and never consults DBConfig's set; ICU registers its session-TimeZone-aware VARCHAR→TIMESTAMP_TZ cast into DBConfig's set (extension_loader.cpp:370-375 → config.GetCastFunctions(); icu-strptime.cpp:510, with VarcharToTimestampTZUS interpreting offset-less strings via the session calendar). So naive inputs are always parsed as UTC regardless of session TimeZone. Reachability: no earlier guard exists on either path — BindAtClause (bind_basetableref.cpp:93-101) binds AT-clause literals untyped via ConstantBinder so a raw VARCHAR reaches hoglake_transaction.cpp:88, and ATTACH SNAPSHOT_TIME passes the raw parser option Value at hoglake_storage.cpp:35; CanonicalInstant refuses nothing. Not a documented design choice: hoglake_types.hpp:29-31, the hoglake_storage.cpp:31-34 comment, and the R2-1 fix disposition (REVIEW-FINDINGS-R2.md:127) all assert 'naive inputs follow the session TimeZone (UTC unless ICU changes it)' — the code makes that claim false, and the consequence is a silent wrong-snapshot read (no error) whenever an ICU-bearing host session sets a non-UTC TimeZone and uses a naive literal, while the identical literal cast at query level means a different instant. The in-tree test build omits ICU (extension_config.cmake loads only hoglake+json), so the suite cannot detect it, but standard DuckDB distributions bundle ICU, making the precondition realistic. Severity medium is correct: silent wrong results, but gated on non-UTC session TZ + naive input (narrower than the in-spec offset-dropping R2-1, which was high).

## R3-3. [MEDIUM] (wire) src/storage/hoglake_storage.cpp:26

**Claim:** SNAPSHOT_VERSION is converted with NumericCast<idx_t>(BigIntValue::Get(...)), which throws InternalException on any negative value, and because the throw happens during ATTACH execution it invalidates the entire DuckDB instance.

**Failure scenario:** Live-reproduced on the branch build: ATTACH 'hoglake:duckext-read' (ENDPOINT ..., SNAPSHOT_VERSION -1) raises 'INTERNAL Error: Information loss on integer cast: value -1' and every subsequent statement on the process fails with 'FATAL Error: database has been invalidated because of a previous fatal error' until restart — a one-character typo in an attach option kills all connections and in-memory state. Sibling sites in the same class: AT (VERSION => -1) at hoglake_transaction.cpp:81 (bind-time, instance survives but users get an assertion-failure INTERNAL error with a stack trace for in-band input) and the maintenance batch parameter at hoglake_metadata_functions.cpp:277.

**Verifier:** CONFIRMED by full path sweep and live reproduction on the branch build. (1) Site exists as claimed: src/storage/hoglake_storage.cpp:26 does `NumericCast<idx_t>(BigIntValue::Get(value.DefaultCastAs(LogicalType::BIGINT)))` on the SNAPSHOT_VERSION attach option. (2) Guard sweep: the entire path — parser accepts -1 as an attach option Value, DatabaseManager::AttachDatabase → HoglakeAttach → HandleHoglakeOption option loop — contains no earlier range check; HandleHoglakeOption is the first hoglake code to touch the value, and DefaultCastAs(BIGINT) passes -1 through untouched. The pin-forces-read-only block (lines 79-85) runs after the option loop, so it never intervenes. (3) Mechanism verified in vendored source: NumericCastImpl throws InternalException (duckdb/src/include/duckdb/common/numeric_utils.hpp:50), and ClientContext invalidates the DatabaseInstance on ExceptionType::INTERNAL (duckdb/src/main/client_context.cpp:368-370, 723-726) — same escalation the R2 verifier confirmed for the puffin finding. (4) Live repro with build/release/duckdb: ATTACH 'hoglake:duckext-read' (ENDPOINT 'http://localhost:8080', SNAPSHOT_VERSION -1) raised exactly 'INTERNAL Error: Information loss on integer cast: value -1 ...' with a stack trace through HoglakeAttach/PhysicalAttach::GetDataInternal, and both subsequent SELECT statements failed with 'FATAL Error: ... database has been invalidated' — instance dead until restart. (5) Sibling site also reproduced as described: SELECT ... FROM lake.ns1.points AT (VERSION => -1) at hoglake_transaction.cpp:81 raises the same INTERNAL error at bind time but the instance survives (SELECT 42 succeeds after). (6) Not fixed and not covered: commits adaa406/49fa194 left the site intact ('overflow hardening' touched other paths), and the only SNAPSHOT_VERSION tests (test/sql/hoglake_read.test:203/272/290) use valid positive pins. (7) Not a documented divergence: DESIGN.md:108 documents only mutual exclusion and forced READ_ONLY for pins; nothing states negative-version semantics, and instance invalidation is not a 'loud error and stated semantics' — it silently destroys all other connections and in-memory state of the process, well beyond rejecting the bad option. Severity medium is correct: pure availability defect requiring user-typed input, but a one-character typo in an attach option kills the whole DuckDB instance (all attached catalogs, temp tables, concurrent connections), and the correct behavior (InvalidInputException/BinderException) is what every neighboring option-validation branch in the same function already does.

## R3-4. [MEDIUM] (catalog) src/storage/hoglake_schema_entry.cpp:185

**Claim:** LoadAllTablesInternal's unrepresentable-table skip catches only CatalogException, so a table whose type conversion throws InvalidInputException (e.g. decimal with missing type_params, accepted by the server) still breaks the entire catalog listing — the exact regression the R2-4 fix ('the LISTING must not break for every other table') claims to prevent.

**Failure scenario:** Live-reproduced: POST /tables with column {"name":"d","type":"decimal"} (no type_params) succeeds server-side; from the extension, SHOW ALL TABLES / duckdb_tables() on the whole attached catalog fails with 'Invalid Input Error: hoglake decimal column "d" is missing precision/scale type_params' (ToDuckDBType, hoglake_types.cpp:51 throws InvalidInputException, not the CatalogException the catch handles), while healthy tables remain reachable only by direct name — catalog introspection is dead for every DuckDB client until out-of-band repair.

**Verifier:** Confirmed by full-path static trace with no earlier guard. Server: CatalogService.createTable validates only identifiers/non-empty/exact-dupes and the OpenAPI ColumnDef makes type_params an optional freeform object (server's own ParquetRewriter defaults missing precision to 38), so POST /tables with {"type":"decimal"} and no type_params succeeds. Client: ParseColumn (hoglake_api_client.cpp:108-112) leaves precision at its default 0 (hoglake_wire.hpp:32); Scan → LoadAllTablesInternal → CacheTableInternal (hoglake_schema_entry.cpp:111) → ToDuckDBType throws InvalidInputException (hoglake_types.cpp:51). The skip at hoglake_schema_entry.cpp:185 catches only CatalogException, which is a sibling class of InvalidInputException in the vendored DuckDB (both derive directly from Exception), so the exception escapes Scan and the whole catalog listing fails, while direct-name lookup of healthy tables still works — exactly as the scenario states. This is not a documented divergence: the code comment and the R2-4 disposition both claim the listing never breaks for unrepresentable tables, and the catch only covers the CI-collision throw site (deliberately CatalogException at line 101), missing the two InvalidInputException throw sites (missing decimal params, unknown type) inside the same CacheTableInternal call. hoglake_case.test exercises only the CI-collision skip. Medium severity is right: loud failure, no silent wrong results, but catalog introspection is dead for all DuckDB clients until out-of-band repair, and the trigger requires an out-of-band client (unlike R2-4's DuckDB-self-inflictable high).

## R3-5. [MEDIUM] (catalog) src/storage/hoglake_schema_entry.cpp:558

**Claim:** The reserved _hog column-name prefix is enforced only on the CREATE TABLE path (line 348); ADD COLUMN and RENAME COLUMN targets have no client-side check and the server does not enforce it either (Identifiers.validate is pattern-only — the extension's and pyhoglake's 'the server 422s them' comments are both false), so ordinary DDL commits user columns named _hog_row_id into the catalog.

**Failure scenario:** Live-reproduced: ALTER TABLE lake.ns1.thog ADD COLUMN _hog_row_id BIGINT and ALTER TABLE ... RENAME COLUMN b TO _hog_marker both succeed and commit server-side (wire shows _hog_row_id as a normal column, field_id 3). Afterward pyhoglake's _check_reserved_columns raises ValidationError on every append to the table (unappendable cross-client), and server compaction's ParquetRewriter.outputSchema appends its reserved _hog_row_id (field id 2147483646) to the live schema, producing a compacted parquet file with two columns named _hog_row_id — breaking the AGENT.md reserved-column contract for any name-based reader.

**Verifier:** Full-path sweep confirms no guard at any layer. Client: the RESERVED_COLUMN_PREFIX check exists only in HoglakeSchemaEntry::CreateTable (hoglake_schema_entry.cpp:348); the ADD_COLUMN (line 558) and RENAME_COLUMN (line 540) cases validate CI collisions/DEFAULT only, RequireDDLAllowed guards buffered-write mixing not names, and HoglakeApiClient::AlterTable serializes the name verbatim. Server: AlterService.addColumn (:159) and renameColumn (:210) call only Identifiers.validate, which is pattern-only (^[A-Za-z_][A-Za-z0-9_-]{0,127}$) and accepts _hog_row_id; grep of the entire server tree finds no reserved-prefix enforcement anywhere (createTable included), and the DB CHECK constraints are the same pattern — so both clients' "the server 422s them" comments are false. Consequences verified: pyhoglake appends fail either via _check_reserved_columns (data carries the column) or _align_table "missing table columns" (it doesn't) — table unappendable cross-client; ParquetRewriter.outputSchema (ParquetRewriter.kt:221) emits liveColumns + ROW_ID_COLUMN yielding two _hog_row_id fields, and on recompaction srcRowIdIndex (line 170, name-based indexOfFirst) would bind the user's column and read user data as row ids — silent corruption. Not a documented divergence: DESIGN.md:300 promises client-side rejection of _hog names, so the missing ALTER check contradicts stated design; tests cover only the CREATE path and neither prior findings file dispositions this. Minor correction: the AGENT.md field-id invariant (2147483646 never allocated) itself survives — the breakage is for name-based binding, as the claim states. Medium severity is right: requires a deliberately _hog-prefixed user column, but ordinary DDL then poisons the shared catalog for all clients and can silently corrupt row ids under compaction.

## R3-6. [MEDIUM] (tests) DESIGN.md:161

**Claim:** DESIGN.md's claim that the buffered-insert invisibility is 'visible in the statement's row count' (and the R2-3 disposition's 'No silent-corruption path remains ... the only surprise (0-row second update) is loud', REVIEW-FINDINGS-R2.md:129) is false: when a later DML statement's predicate PARTIALLY overlaps a value an earlier same-txn statement rewrote, the second statement reports a plausible nonzero count while silently missing the rewritten rows, and COMMIT persists data that violates sequential statement semantics.

**Failure scenario:** t={1,2,3} in one file. BEGIN; UPDATE t SET a=3 WHERE a=1 (reports 1, buffers insert {3}, masks pos0); DELETE FROM t WHERE a=3 (scan sees {2,3}, deletes only the original 3, reports 1 — nonzero and plausible); COMMIT. Committed result {2,3}: a row matching the DELETE predicate survives with no 0-count signal anywhere (sequentially correct result is {2}). Same shape: UPDATE ... SET status='done' WHERE id=5; DELETE ... WHERE status='done' silently leaves the just-updated row. hoglake_txn.test only replays the fully-overlapping repros whose second statement reports 0, so the loudness claim exceeds what any test proves.

**Verifier:** Confirmed in code. (1) Buffered inserts are invisible: HoglakeMultiFileList builds the scan file list solely from server-side files at the pinned snapshot (no append awareness anywhere in hoglake_multi_file_list.cpp), while hoglake_multi_file_reader.cpp:76-97 merges only buffered DELETE positions into the mask. (2) UPDATE is delete-positions + buffered append (hoglake_update.cpp), so its rewrites are unreadable by later statements. (3) Full guard sweep: RequireDMLAllowed (hoglake_transaction.cpp:210-243) refuses only DDL+DML mixes; RequireDDLAllowed only DDL-over-buffered-writes; no layer refuses UPDATE-then-DELETE on one table — hoglake_txn.test itself runs multi-DML same-table transactions, so the scenario is reachable end to end, and commit ships appends+deletes with no re-validation. (4) The arithmetic holds: UPDATE a=1→3 reports 1; DELETE WHERE a=3 sees {2,3}, deletes only original pos2, reports 1 (nonzero, plausible); COMMIT yields {2,3} where sequential semantics require {2} — a row matching the DELETE predicate survives silently. (5) The loudness claims are demonstrably false for partial overlap: DESIGN.md:158-164 ("visible in the statement's row count") and R2-3 disposition ("only surprise (0-row second update) is loud"; "No silent-corruption path remains") hold only when the second predicate matches exclusively rewritten rows; hoglake_txn.test:208-221 replays only unpredicated second statements, so tests prove nothing about the partial-overlap case. Under this round's ground rule, a documented divergence is a finding when it produces silent wrong results — this one commits wrong data with no 0-count or error anywhere. Severity medium is appropriate: silent persisted corruption of sequential semantics, gated on a multi-statement DML txn whose later predicate overlaps a value rewritten earlier in the same txn.

## R3-7. [MEDIUM] (tests) src/common/hoglake_types.cpp:134

**Claim:** The R2-1 disposition (REVIEW-FINDINGS-R2.md:127) and the CanonicalInstant contract comment (src/include/common/hoglake_types.hpp:29-30) state 'naive inputs follow the session TimeZone (UTC unless ICU changes it)', but the implementation uses Value::DefaultCastAs, which constructs a bare CastFunctionSet (duckdb/src/common/types/value.cpp:2159-2162) that never consults DBConfig/session settings — naive inputs are ALWAYS parsed as UTC, so the documented semantics are false and diverge from the engine's own ICU cast behavior.

**Failure scenario:** User with ICU loaded and SET TimeZone='Europe/Berlin' runs SELECT ... AT (TIMESTAMP => TIMESTAMP '2026-06-01 12:00:00') (or SNAPSHOT_TIME with a naive string). Per the documented contract and per DuckDB's own TIMESTAMP→TIMESTAMPTZ cast semantics under ICU, this denotes 10:00Z; the client sends at_timestamp=12:00:00Z — two hours off — silently pinning a different snapshot, the exact silent wrong-snapshot class R2-1 was rated HIGH for. No test runs with a non-UTC session TimeZone (all fixture timestamps are UTC wall clock), so the suite cannot distinguish the documented behavior from the implemented one.

**Verifier:** CONFIRMED. (1) Mechanism: Value::DefaultCastAs (vendored value.cpp:2159-2162) builds a bare CastFunctionSet with no DBConfig; the default TIMESTAMP→TIMESTAMP_TZ cast it resolves is ReinterpretCast (time_casts.cpp:122-124), i.e. naive is always treated as UTC. ICU's session-TimeZone-aware replacement cast is registered into the DBConfig cast set (icu-timezone.cpp:194) and is therefore never consulted — so with ICU + SET TimeZone='Europe/Berlin', the engine's own TIMESTAMP'12:00:00'::TIMESTAMPTZ yields 10:00Z while CanonicalInstant (hoglake_types.cpp:134) sends 12:00Z. (2) Reachability swept end-to-end with no earlier guard: Binder::BindAtClause (bind_basetableref.cpp:93-101) evaluates the AT expression at its natural type with no cast, so naive TIMESTAMP/VARCHAR Values reach TravelFor (hoglake_transaction.cpp:88) and CanonicalInstant untouched; the SNAPSHOT_TIME attach option (hoglake_storage.cpp:35) feeds the raw parsed literal in directly; no refusal rule or validation intercepts either path. (3) Contract mismatch: hoglake_types.hpp:29-30, the comment at hoglake_storage.cpp:31-34, and the R2-1 disposition all assert 'naive inputs follow the session TimeZone (UTC unless ICU changes it)' — false; implementation is always-UTC. This is not a documented-loud divergence: the docs state the opposite of the behavior and the result is a silently wrong pinned snapshot (the same silent wrong-snapshot class R2-1 was HIGH for), consistent with the ducklake-fork reference which uses the context-aware val.CastAs(context, TIMESTAMP_TZ) (ducklake_metadata_manager.cpp:4984). (4) Coverage gap confirmed: grep over test/ finds no TimeZone or icu usage; all fixture instants are UTC, so the R2-1 regression tests exercise only the explicit-offset branch and cannot detect this. Severity medium is calibrated: requires ICU (bundled in standard builds) plus a non-UTC session TimeZone plus a naive input — narrower than R2-1 — but the failure is silent wrong-snapshot data with no error.

## R3-8. [MEDIUM] (tests) PARITY.md:20

**Claim:** PARITY.md:20 claims ambiguity errors for case-colliding 'tables, columns, and namespaces' are exercised via fixture-created colliding pairs, and the R2-4 disposition (REVIEW-FINDINGS-R2.md:130) excuses the untested passive column-collision defense as needing 'another client mutating the shared catalog mid-suite' — both statements are false: read_fixture.py creates colliding TABLE and NAMESPACE pairs at fixture time via pyhoglake but no case-colliding-COLUMNS table, which is creatable the identical way (server dedupe is exact-match, CatalogService.kt:185-187), so the CacheTableInternal column guard (hoglake_schema_entry.cpp:97-107) and the LoadAllTablesInternal skip (183-190) have zero test coverage.

**Failure scenario:** Revert the round-2 column-collision guard (or introduce a regression in the CatalogException-catch listing skip): all 515 assertions stay green because no fixture table has CI-colliding columns, while a pyhoglake-created table with columns 'team'/'Team' makes SELECT, DROP, and SHOW ALL TABLES throw from ColumnList for the whole attached catalog — the exact catalog-wide breakage R2-4 rated HIGH, shipping undetected.

**Verifier:** CONFIRMED at every link by code inspection. (1) PARITY.md:20 claims "ambiguity errors for case-colliding server-side tables, columns, and namespaces with listings surviving — fixture-created colliding pairs exercised (hoglake_case.test)" — but read_fixture.py:211-223 creates only a table pair (ambig_t/Ambig_T) and a namespace pair (ambigns/AmbigNs); no fixture table has CI-colliding columns, so the "columns" third of the claim is exercised by nothing. (2) The R2-4 disposition rationale (REVIEW-FINDINGS-R2.md:130, "constructing it needs another client mutating the shared catalog mid-suite") is false: a persistent colliding-column table is creatable at fixture time via pyhoglake exactly like the two pairs the overflow-#4 fix added in the same file — pyhoglake create_table (client.py:458-465) only checks reserved columns and sends the schema verbatim, and server createTable dedupes exact-match only (CatalogService.kt:185-187, groupingBy{it.name}); R2-4's own live repro already proved a mixed-case 'Team' column commits server-side, so no layer refuses the precondition. (3) Zero coverage confirmed by grep: the CacheTableInternal column guard (hoglake_schema_entry.cpp:97-107, error "columns whose names differ only by case ... Repair the table via another client") and the LoadAllTablesInternal CatalogException-catch skip (:183-190) are hit by no test — the only "differ only by case" assertion in the suite (hoglake_case.test:158) is the TABLE-ambiguity error from ResolveTableNameInternal (:69-72), and the only "Team" occurrences (hoglake_case.test:118,123) are the ACTIVE ADD/RENAME COLUMN refusals whose client-side guard fires before any server commit, so they never create a colliding-column table. The failure scenario is sound: reverting the :97-107 guard (or regressing the :183-190 catch) triggers no fixture input, so all 515 assertions stay green while a pyhoglake-created team/Team table reproduces the exact catalog-wide breakage (SELECT/DROP/SHOW ALL TABLES all throw from ColumnList via CacheTableInternal/LoadAllTablesInternal) that R2-4 live-reproduced and rated HIGH. Severity medium is correctly calibrated: pure coverage-gap-plus-false-documentation defect shielding a HIGH breakage class, matching the round-2 precedents R2-11 and R2-12 (both medium for exactly this class). This is not a documented-loud divergence — the guard itself is loud, but the finding is that the fix-verification claims are false and the guard is unprotected against regression.

## R3-9. [MEDIUM] (tests) src/storage/hoglake_transaction.cpp:235

**Claim:** RequireDMLAllowed refusal rule (c) — INSERT into an eagerly-ALTERed table while buffered deletes exist — has no test: grep finds no test triggering its 'cannot INSERT into table' error, hoglake_txn.test covers only rules (a) and (b), yet the R2-2 disposition cites hoglake_txn.test for the rule set and PARITY.md:52 marks 'DDL vs buffered writes (both orders)' DONE (defined at PARITY.md:5 as implemented AND tested).

**Failure scenario:** Revert rule (c) (the hoglake_transaction.cpp:235-241 branch): the suite stays green. A user then runs BEGIN; DELETE FROM lake.ns1.y WHERE ...; ALTER TABLE lake.ns1.x ADD COLUMN c INT; INSERT INTO lake.ns1.x VALUES (...); COMMIT — the commit carries deletes, the server conflict-scans table_altered over all touched tables including x, and deterministically 409s against the transaction's own ALTER: never-retryable abort with the ALTER persisted and all DML lost, the exact half-applied-transaction class R2-2/R2-5 were filed for.

**Verifier:** Confirmed. (1) Rule (c) (hoglake_transaction.cpp:233-241, sole caller hoglake_insert.cpp:223) has zero test coverage: grep over the entire tree finds its "cannot INSERT into table" error only in the two source files; hoglake_txn.test asserts only rule (a) (line 248) and rule (b) (line 286), and the only ALTER-then-INSERT test (lines 253-269) runs with empty buffered_deletes, exercising the non-firing side. No test issues DELETE→ALTER→INSERT, so reverting the branch leaves the suite green by construction. (2) The counterfactual scenario is real with no other guard: RequireDDLAllowed only blocks DDL on tables with their own buffered writes (deletes on y don't block ALTER x); Commit has no client-side touched∩altered check, sends the pre-ALTER pinned read_snapshot when deletes exist, and treats any deletes-carrying 409 as never-retryable (hoglake_transaction.cpp:324-330); the server-side deterministic self-409 over all touched tables including appends was live-reproduced and CONFIRMED in R2-2. Result: eager ALTER persisted, all DML lost, sequence permanently uncommittable — the exact R2-2/R2-5 half-applied class. (3) The documentation claims are false as stated: REVIEW-FINDINGS-R2.md:128 cites hoglake_txn.test as evidence for the a/b/c rule set, and PARITY.md:52 marks the capability DONE where DONE is defined (PARITY.md:4-5) as implemented AND tested. Severity medium matches series precedent: R2-11 (untested enforcement path) and R2-5 (false disposition text) were both rated medium, and the guarded failure mode is the confirmed-medium R2-2 class.

## R3-10. [LOW] (txn) DESIGN.md:209

**Claim:** The 'Transactions and eager DDL' section still states 'Read-your-own-writes is NOT provided: uncommitted inserts/deletes are invisible to the transaction's own scans', contradicting the round-2 R2-3 fix documented 50 lines earlier (DESIGN.md:153-165, 'Read-your-own-DELETES ... every same-transaction scan merges the transaction's buffered delete positions') — the stale sentence describes exactly the pre-fix semantics whose consequence was silent committed corruption.

**Failure scenario:** A client developer (or a future fix wave) reading the transactions section at line 209 concludes buffered deletes are invisible to same-transaction scans and that DELETE-then-UPDATE re-reading deleted rows is intended behavior; code changed to match the stated contract (e.g. dropping the MergePositions merge in InitializeReader as 'unnecessary') would reintroduce the R2-3 silent-corruption class (resurrected deletes, doubled rows) while conforming to the document. No runtime defect today; the two passages cannot both be true.

**Verifier:** Confirmed. DESIGN.md:209 ("uncommitted inserts/deletes are invisible to the transaction's own scans") directly contradicts DESIGN.md:153-164 ("Read-your-own-DELETES... every same-transaction scan merges the transaction's buffered delete positions into its delete mask"), and the code implements the latter: hoglake_multi_file_reader.cpp:76-97 merges GetBufferedDeletePositions via HoglakeDeleteFilter::MergePositions for every non-travel-pinned same-transaction scan, exactly as R2-3's FIXED disposition (REVIEW-FINDINGS-R2.md:129) records. git blame shows the mechanism: line 209 dates from the round-1 fix commit adaa406 and was never revised when the round-2 fix commit 49fa194 added the read-your-own-DELETES section at line 153 — the "/deletes" word is a stale leftover describing pre-fix semantics whose violation previously caused silent committed corruption (resurrected deletes, doubled rows). No consistent reading reconciles the two passages, and this is not a documented-loud divergence — it is a false statement about implemented behavior in the contract document. No runtime defect exists today (the reviewer concedes this); the harm path is a future developer or fix wave treating line 209 as the contract and removing the merge. Severity low is correct for a doc-only inconsistency with speculative future impact.

## R3-11. [LOW] (catalog) src/storage/hoglake_catalog.cpp:47

**Claim:** HoglakeCatalog::Initialize executes the CreateCatalog control-plane mutation (POST /catalogs) with no access-mode check, so a READ_ONLY attach — including SNAPSHOT_VERSION/SNAPSHOT_TIME pins the extension itself forces read-only — creates a catalog server-side, another mutating surface missed by the R2-9 access-mode fix (which covered only the maintenance functions).

**Failure scenario:** Live-reproduced: ATTACH 'hoglake:duckext-r3ro' (ENDPOINT ..., READ_ONLY, CREATE_IF_NOT_EXISTS true, DATA_PATH 's3://duckext-itest/r3ro/') and the pinned variant ATTACH (SNAPSHOT_VERSION 5, CREATE_IF_NOT_EXISTS true, ...) both succeeded and GET /catalogs afterward shows duckext-r3ro and duckext-r3ro2 created — a read-only attach performed a persistent server-side mutation (the pinned one additionally pins a snapshot that does not exist in the just-created catalog).

**Verifier:** Confirmed by full-path sweep plus live evidence. HoglakeAttach (src/storage/hoglake_storage.cpp:57-88) has no guard pairing create_if_not_exists with access_mode; HoglakeCatalog::Initialize (src/storage/hoglake_catalog.cpp:34-52) calls api_client->CreateCatalog unconditionally when the catalog is missing and CREATE_IF_NOT_EXISTS+DATA_PATH are set; HoglakeApiClient::CreateCatalog (src/rest/hoglake_api_client.cpp:402-408) POSTs /v1/catalogs unconditionally; DuckDB core invokes Catalog::Initialize during attach regardless of read-only. The R2-9 fix guard exists only in MaintenanceBind (src/functions/hoglake_metadata_functions.cpp:265-273) — grep confirms no IsReadOnly/access_mode check anywhere on the attach path. Live server corroborates the repro: GET /v1/catalogs shows duckext-r3ro and duckext-r3ro2 with head_snapshot_id 0 / schema_version 0 (freshly created by the read-only and snapshot-pinned attaches; the pinned one references snapshot 5 in a head-0 catalog). Not a documented divergence (DESIGN.md:109 documents the mapping but not the read-only interplay) and the mutation is silent, so it is a defect under the round's rules. Severity low is appropriate: requires explicit CREATE_IF_NOT_EXISTS from the user and creates an empty catalog (persistent, contract-violating, but non-destructive, unlike the medium R2-9 expire path).

## R3-12. [LOW] (catalog) src/storage/hoglake_schema_entry.cpp:148

**Claim:** Once all_tables_loaded is set by any catalog listing, LookupTableInternal returns nullptr for a table whose entry construction failed (case-colliding columns), so direct lookup reports 'Table does not exist' instead of the R2-4 fix's targeted 'repair via another client' error, and DROP TABLE IF EXISTS silently no-ops on a table that exists server-side.

**Failure scenario:** Live-reproduced: with fixture table tcase (columns team/Team created via REST), a fresh transaction's SELECT * FROM lake.ns1.tcase gives the targeted CatalogException, but BEGIN; SELECT count(*) FROM duckdb_tables(); SELECT * FROM lake.ns1.tcase; gives 'Catalog Error: Table with name tcase does not exist!' and BEGIN; <listing>; DROP TABLE IF EXISTS lake.ns1.tcase; returns success without dropping anything — the listing's skip-on-failure is conflated with 'listed at head but not visible at the pinned travel', making the table's existence and its repair hint order-dependent within a transaction.

**Verifier:** Confirmed by code inspection and live reproduction. LoadAllTablesInternal (hoglake_schema_entry.cpp:183-190) catches the colliding-column CatalogException, skips the table, and sets all_tables_loaded; LookupTableInternal then returns nullptr at line 148 for that table (still present in table_names, absent from tables), never reaching the CacheTableInternal call that surfaces the R2-4 targeted 'repair via another client' error. Live repro against dev server (duckext-r3v/ns1/tcase with columns team/Team, confirmed via REST): fresh transaction gives the targeted CatalogException; BEGIN + duckdb_tables() + SELECT gives 'Table with name tcase does not exist!' (reproduced with SNAPSHOT_VERSION 12 pin to bypass an unrelated tdec100 InternalException in the same catalog). DropEntry:379-382 returns silently on RETURN_NULL before any API call, so DROP TABLE IF EXISTS reports success while the table exists server-side — code-certain from the same nullptr. No earlier guard refuses the input, and neither DESIGN.md nor the R2-4 disposition documents this order-dependence — both assert direct lookup surfaces the targeted error. Severity low stands: no data corruption and the table is unusable from DuckDB in either ordering, but the existence misreport and silent DROP IF EXISTS success are wrong results, not degraded diagnostics.

## R3-13. [LOW] (tests) src/storage/hoglake_multi_file_reader.cpp:95

**Claim:** No test exercises the combined branch of the R2-3 scan-side fix where a file has BOTH a committed server-live deletion vector (Initialize) and same-transaction buffered deletes (MergePositions): every read-your-own-deletes test uses freshly-created tables with no committed DV, and the one in-txn DELETE on a DV-carrying table (hoglake_txn.test:96) is rolled back with no subsequent scan.

**Failure scenario:** Degrade MergePositions (hoglake_delete_filter.cpp:24-33) from set_union to plain overwrite (deleted_rows = positions): the entire suite passes, because in every test that reaches MergePositions the server-live DV set is empty so overwrite equals union. In production, BEGIN; DELETE ...; UPDATE ... on a table with previously committed deletes would then scan with only the buffered positions, resurrect committed-deleted rows through the UPDATE's rewrite, and COMMIT silently corrupted data — a regression in the round-2 HIGH fix's core merge that the suite cannot detect.

**Verifier:** Confirmed by static trace of every MergePositions invocation the suite can reach. The combined branch at hoglake_multi_file_reader.cpp:89-96 (has_delete_file → Initialize committed DV, plus non-empty buffered → MergePositions) is never exercised: (1) all read-your-own-deletes txns (hoglake_txn.test lines 48-66, 187-200, 213-226) run on freshly created tables with no committed DV, so MergePositions always merges into an empty base where overwrite==union; (2) every in-txn DELETE on a DV-carrying table (lines 96, 149, 246, 284) either has no subsequent scan before ROLLBACK or is refused by the DDL rule, and a DELETE's own scan initializes readers before positions are buffered; (3) autocommit DELETEs (hoglake_dml_alter.test:62) exercise the commit-side DV merge, not the scan-side one; (4) no unit tests reference HoglakeDeleteFilter and the pyhoglake fixtures don't do in-txn DML. Guard sweep: RequireDMLAllowed/RequireDDLAllowed (hoglake_transaction.cpp:210-254) refuse only DDL-mixing shapes, so the production scenario BEGIN; DELETE; UPDATE on a table with a previously committed DV is legal and reaches the untested branch, where a union→overwrite regression would drop committed DV positions and commit resurrected rows silently. Severity low is right: it is a coverage gap in the core merge of the round-2 HIGH fix, but the shipped code is currently correct.


## Unverified (verifier failed on transient error — assess as if confirmed until refuted)

- [medium] src/storage/hoglake_delete.cpp:80 — The DELETE sink attributes deleted positions to data files by PATH (by_path.emplace keeps the first entry; gstate.new_deletes is keyed by the scan's FILENAME column), but the catalog legally holds multiple live logical files registered for one physical path (CommitService.kt admits duplicate paths against live rows by design for writer retries — no dedup in validateFiles; a second registration of an already-live path was accepted live, snapshot 81), so the superseding DV is registered against only the FIRST data_file_id and the other logical file's rows silently survive a successful committed DELETE.
  Scenario: Live-reproduced on the dev stack (scratch catalog duckext-sqltest): table ns1.r3_dup with one 3-row file registered twice (data_file_ids 43 and 45, same path, row_id_start 0/3) reads as {1,1,2,2,3,3}; DELETE FROM lake.ns1.r3_dup WHERE a = 1 succeeds and commits, but the post-commit table is {1,2,2,3,3} — a row matching the predicate remains visible because the DV for the collapsed path was registered on id 43 only, and the reported deleted count (deduped per (path,row)) undercounts the matching rows the user saw. Precondition is externally created but explicitly in-contract (e.g. a pyhoglake writer retrying an ambiguous commit that had already landed); the extension reads such a table without complaint (doubled rows), then half-applies DELETEs/UPDATEs against it silently. Same-transaction scans inherit the mis-attribution: GetBufferedDeletePositions is keyed by data_file_id, so the duplicate logical file's rows also stay unmasked for later statements.

---

# Round-3 fix dispositions (2026-09-12, this branch)

All 13 confirmed findings fixed; the unverified finding was treated as
confirmed, independently re-verified live, and fixed. Suite after
fixes: `./test/run-live-tests.sh` green — 11 sqllogictest files / 592
assertions + cross-client partition-wire check. NOTE: the dev stack's
hydrator/compactor came back to life this round, which turned two
previously-untestable paths (real compaction output, explicit
`_hog_row_id` reads) into tested ones.

## The InternalException class (R3-1, R3-3, R3-4 + mandated sweep)

Ground rule implemented: no wire- or user-supplied value may reach an
InternalException (DuckDB invalidates the whole instance on
ExceptionType::INTERNAL). Full inventory of all 45 NumericCast sites
plus every LogicalType constructor was classified and dispatched:

- **Wire-supplied** — parse-time validation in the API client
  (`GetBoundedInt`): every numeric field of DataFile/DeleteFile/
  Catalog/Snapshot/Column/PartitionField parses through explicit
  bounds and throws a catchable IOException on violation; decimal
  type_params are additionally validated in ParseTableInfo with a
  CatalogException NAMING THE TABLE (belt) and re-validated in
  ToDuckDBType (braces) before LogicalType::DECIMAL can see them.
  Downstream NumericCasts on these fields are now provably in-range.
- **User-supplied** — typed errors before any cast: SNAPSHOT_VERSION
  (InvalidInputException; previously an ATTACH typo killed the whole
  instance, live-reproduced), AT (VERSION => n) (BinderException),
  maintenance `batch` (BinderException), bucket(n) counts
  (BinderException), page_size (already validated).
- **Engine-internal** — the remaining sites take values produced by
  the engine or by this extension (scan row numbers, copy statistics,
  set sizes, puffin writer sizes, FileHandle::GetFileSize which is
  already idx_t) and were left as internal invariants.

| # | Disposition |
|---|---|
| R3-1 (HIGH, decimal instance-kill) | FIXED (see sweep). Live-verified: SELECT on the fixture's precision-100 table gives the targeted CatalogException naming table and column, the instance survives, and SHOW ALL TABLES / duckdb_tables / hoglake_table_info all skip it. Tests: `hoglake_wire_hardening.test` (poison tables bad_dec100 / bad_dec_nop created via raw REST in the fixture). |
| R3-2 + R3-7 (MED, session-TimeZone claim false) | FIXED by implementing the documented semantics (the preferred option): `CanonicalInstant` now takes the ClientContext and casts via `Value::CastAs(context, TIMESTAMP_TZ)`, which consults DBConfig's cast set — ICU's session-aware cast applies when loaded. Both call sites (AT clause via the transaction's context; SNAPSHOT_TIME via the attach context) thread the context. Docs/dispositions corrected to state precisely: naive inputs mean what the session's ::TIMESTAMPTZ cast means — session TimeZone under ICU, UTC in ICU-less builds; this repo's test build has no ICU, so the ICU branch is correct by construction of the engine cast, not suite-verified (stated in the R2-1 correction). |
| R3-3 (MED, SNAPSHOT_VERSION -1 instance-kill) | FIXED (see sweep). Live-verified: the attach now raises InvalidInputException and the instance survives; sibling sites (AT VERSION, batch) verified alive too. Tests in `hoglake_wire_hardening.test` follow every refusal with a liveness query. |
| R3-4 (MED, listing catch too narrow) | FIXED. The listing skip now covers BOTH throw layers — the wire fetch (parse-time validation throws there since this round) and entry construction — catching CatalogException and InvalidInputException; `hoglake_table_info` got the same containment. Live-verified against all three poison tables; tested. |
| R3-5 (MED, _hog on ALTER targets) | FIXED. ADD COLUMN and RENAME COLUMN targets reject the reserved prefix (case-insensitively); the false "the server 422s them" comment is corrected — the client is the enforcement point, and server finding 9 asks the server to enforce it too. Tests: `hoglake_wire_hardening.test`. |
| R3-6 (MED, partial-overlap silent wrongness) | FIXED by extending the refusal family: DELETE/UPDATE on a table this transaction already appended rows to (INSERT or UPDATE rewrites) is refused loudly — the invisible-insert semantics cannot produce a silently-wrong commit in any predicate shape. DELETE-then-DELETE/UPDATE remain legal. The false "visible in the statement's row count" claims are corrected in DESIGN.md and the R2-3 disposition. Tests: `hoglake_txn.test` replays the exact R3-6 partial-overlap shape (now refused), the UPDATE-then-UPDATE shape (refused), INSERT-then-DELETE (refused), and the still-legal orders. |
| R3-8 (MED, colliding-column coverage claims false) | FIXED. Fixture creates `bad_cols` (team/Team) via raw REST; `hoglake_wire_hardening.test` exercises the targeted error, the listing skip, and order independence. PARITY and the R2-4 disposition corrected in place. |
| R3-9 (MED, rule (c) untested) | FIXED. `hoglake_txn.test` now runs DELETE y → ALTER x → INSERT x and asserts the rule-(c) refusal. |
| R3-10 (LOW, stale DESIGN sentence) | FIXED. The "uncommitted inserts/deletes are invisible" leftover is rewritten: deletes ARE masked, inserts are invisible, and hazardous DML is refused; the two passages now agree. |
| R3-11 (LOW, create-catalog under READ_ONLY) | FIXED at two layers: the attach refuses CREATE_IF_NOT_EXISTS combined with READ_ONLY or a snapshot pin (typed error), and Initialize defensively refuses the POST from a read-only attach. Live-verified (no catalog created); tested. |
| R3-12 (LOW, stale-negative-cache lookup) | FIXED with a coherent story: tables a listing (or cold lookup) skipped as unrepresentable are remembered per-transaction (`unrepresentable_tables`, exact name → the raw targeted message); a later direct lookup RETHROWS that error instead of "does not exist", and DROP TABLE IF EXISTS errors loudly instead of silently no-opping. Order-independence tested (listing-then-lookup in one transaction, `hoglake_wire_hardening.test`). |
| R3-13 (LOW, combined DV branch untested) | FIXED. `hoglake_txn.test` adds a table with a COMMITTED deletion vector, then BEGIN; DELETE; UPDATE; COMMIT — the UPDATE's scan must apply committed-DV ∪ buffered positions (a union→overwrite regression in MergePositions resurrects the committed-deleted row and fails the assertion). |

## Unverified finding (duplicate-path registrations) — CONFIRMED, FIXED

Treated as confirmed per the mandate and independently re-verified:
the fixture registers one physical parquet as two live logical files
(raw REST re-commit of the same path — the wire admits it for writer
retries), and pre-fix the DELETE's superseding DV landed on only the
first data_file_id, leaving the duplicate's rows visible. FIXED: the
delete sink groups scan files by path and registers the merged
superseding DV for EVERY live data_file_id of that path (each with its
own per-id existing-DV/buffered merge and its own puffin object).
Live-verified and tested (`hoglake_wire_hardening.test`: dup_path
starts {1,1,2,2,3,3}; DELETE a=1 leaves no copy of 1 and both copies
of 2 and 3). Residual note: the statement's reported delete count
dedupes by (path, position), so it counts logical rows once even when
the user saw doubled rows — recorded here rather than hidden.

## Environment change this round

The dev stack's hydrator/compactor work again (stats hydrate to
`provided`; compaction produces explicit-row-id outputs). The fixture
now force-compacts `points` (bounded wait + POST /maintenance/compact)
and exports DUCKEXT_POINTS_COMPACTED; `hoglake_compacted_read.test`
deterministically reads a real compaction output — one merged file,
row ids preserved 0..4 through the physical `_hog_row_id` column, with
the pre-compaction layout still reachable via time travel. Suite
layout assertions that a background compaction could invalidate were
pinned AT fixture-exported snapshots; verify_partition_wire.py accepts
compacted (single-file) groups since group-collapse is itself proof of
byte-identical partition strings.
