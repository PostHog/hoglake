# Adversarial review findings — round 4 (2026-09-12)

11 confirmed of 17 raw; 0 refuted.

## R4-1. [HIGH] (memory) src/storage/hoglake_delete.cpp:123

**Claim:** The R3 per-path multi-DV loop throws an unguarded InternalException (whole-instance invalidation) when merged delete positions reach a logical file's server-declared record_count, which externally-supplied catalog/DV data can always cause: the server admits duplicate-path registrations with inconsistent record_count (validated only >= 0, never against the parquet or sibling registrations) and never reads committed DV contents (only delete_count <= record_count is checked), so both position sources at lines 113-120 can legally exceed the record_count read at line 121.

**Failure scenario:** Live-reproduced on the dev stack: table ns1.dup3 with one 3-row parquet registered twice (extension INSERT with record_count 3, then a raw-REST commit of the same path with record_count 2 — server accepted it, snapshot 4). DELETE FROM lake.ns1.dup3 WHERE a = 3 (physical ordinal 2) dies in HoglakeDelete::Finalize with 'INTERNAL Error: hoglake: deleted position 2 out of range for data file ... (2 rows)', and every subsequent statement in the process fails 'FATAL Error: database has been invalidated' until restart. Same trigger via a committed DV whose positions exceed record_count (written by any other client). Violates the R3 sweep's own ground rule that no wire/storage-supplied value may reach an InternalException — the sweep misclassified this site as engine-internal.

**Verifier:** CONFIRMED by full guard sweep plus independent live reproduction. (1) Site: /Users/jakob/src/hoglake-duckdb-client/duckdb-client/src/storage/hoglake_delete.cpp:122-125 throws InternalException when the merged position set's max >= record_count; positions come from the scan's file_row_number ordinals (line 54), the committed DV (line 115-116), and buffered positions (118-120), while record_count (line 121) is wire-supplied. (2) Guard sweep found NO earlier refusal on any layer: client parse bounds are only [0, MAX_I64] (hoglake_api_client.cpp:236 GetBoundedInt); the server's validateFiles (CommitService.kt:875-885) checks only 0 <= record_count <= 2^48 per file, never reads the parquet, and admits duplicate-path registrations with inconsistent record_count by design; committed DV contents are never validated (server checks only deleteCount <= recordCount at CommitService.kt:723; ReadDeletionVector in hoglake_puffin.cpp decodes with no range check; hoglake_delete_filter.cpp does not validate either); the scan passes only file_size/footer_size/row_id_start to the parquet reader (hoglake_multi_file_list.cpp GetFile), so the reader yields actual-file ordinals regardless of declared record_count, and the R3 per-path loop applies the merged set to EVERY registration of the path including an under-declared sibling. (3) Sweep misclassification verified: REVIEW-FINDINGS-R3.md's InternalException inventory left 'scan row numbers ... set sizes' as engine-internal, but the comparison bound (record_count) is wire data — this violates the sweep's own ground rule; no fix wave touched the site and DESIGN.md documents no divergence. (4) Live repro (the reviewer's ns1.dup3 artifact was absent from the dev server, so I rebuilt it in scratch catalog duckext-r4vfy2): extension INSERT of (1,2,3) registered the parquet with record_count 3; a raw-REST commit of the same path with record_count 2 was accepted (snapshot 4, exactly as claimed); DELETE FROM lake.ns1.dup3 WHERE a = 3 then died in HoglakeDelete::Finalize with 'INTERNAL Error: hoglake: deleted position 2 out of range for data file ... (2 rows)' and both subsequent SELECTs failed 'FATAL Error: ... database has been invalidated' until restart. Severity high is correct per the round's own rule: an unguarded InternalException reachable from externally supplied catalog/DV data is automatically high — one in-contract duplicate registration (or any other client's DV whose positions exceed a declared record_count) kills the entire DuckDB instance, all attached catalogs and connections included. (Scratch table dropped after the repro; the empty scratch catalog remains, as catalog DELETE is unsupported.)

## R4-2. [HIGH] (wire) src/storage/hoglake_delete.cpp:122

**Claim:** The delete sink's position-bounds check throws InternalException on wire/object-store-supplied data: the merged position set includes the existing DV read from puffin (WritePuffinFile inputs at line 115), and neither the server (commit validates only delete_count <= record_count; the footer-shipping contract never opens DVs) nor the scan-side filter (HoglakeDeleteFilter::Filter silently ignores out-of-range positions) bounds DV positions against record_count, so an in-contract external registration reaches an instance-invalidating throw.

**Failure scenario:** Another client registers a structurally valid puffin DV for a 3-row data file with positions {5} and delete_count 1 (<= record_count, accepted by CommitService.applyDeletes which checks counts only). SELECTs on the table work (Filter skips position 5). The user then runs DELETE or UPDATE on the table: Finalize unions the existing DV into positions, *positions.rbegin()=5 >= record_count=3, and the InternalException at line 123 fires — DuckDB invalidates the entire instance (every catalog, every connection) until restart. This is the same external-data-to-InternalException class as R3-1, sitting inside the round-3 duplicate-path DV fix seam, and the R3 sweep's ground rule ('no wire-supplied value may reach an InternalException') is violated by it.

**Verifier:** Confirmed by full guard sweep from external registration to the throw site. (1) Server commit (CommitService.kt:723) validates only deleteCount <= recordCount and never decodes the puffin blob, so a structurally valid DV with positions {5}, delete_count 1 on a 3-row data file commits in-contract. (2) Client parse-time bounds (hoglake_api_client.cpp:280) bound delete_count to [0, MAX_I64] only; DV positions are in the object-store blob, never seen by GetBoundedInt. (3) HoglakePuffin::ReadDeletionVector/DecodeBlob validate structure only (magic/CRC/length/negative bucket key), all via catchable InvalidInputException; position values are unbounded. (4) Scan-side HoglakeDeleteFilter::Initialize checks only positions-count == delete_count (consistent for the hostile DV), and Filter silently passes rows when the deleted position exceeds every scanned range — SELECTs work, hiding the poison. (5) RequireDMLAllowed's refusal family is orthogonal; a fresh-transaction DELETE passes. (6) hoglake_delete.cpp Finalize (line 115) unions the existing DV into positions and line 122's bound check throws InternalException at line 123 (5 >= 3); DuckDB invalidates the whole instance on ExceptionType::INTERNAL (mechanism live-confirmed in R3 verifications). UPDATE shares the path via delete_op.Finalize (hoglake_update.cpp:156). Not fixed by any wave: the R3 sweep inventoried NumericCast/LogicalType sites and classified remaining set-based sites as engine-internal, missing that this set's contents include object-store-supplied positions. Not a documented divergence (DESIGN.md has no out-of-range-DV caveat; the codebase pattern for untrusted DV data is InvalidInputException). Only correction: the throw is at line 123, condition at 122. High severity per the ground rule: wire/object-store-supplied data reaching an unguarded InternalException equals whole-instance invalidation.

## R4-3. [HIGH] (wire) src/storage/hoglake_table_entry.cpp:107

**Claim:** GetStorageInfo does NumericCast<idx_t>(table_info.record_count), but ParseTableInfo (hoglake_api_client.cpp:176-178) parses the Table schema's record_count/file_count/file_size_bytes with unbounded GetInt instead of GetBoundedInt — the Table schema was omitted from the R3 parse-bounds sweep (its disposition inventory lists DataFile/DeleteFile/Catalog/Snapshot/Column/PartitionField but not Table), so a negative wire record_count reaches an InternalException.

**Failure scenario:** A GET-table response with record_count -1 (malformed or compromised server/proxy — exactly the input class GetBoundedInt exists to convert into catchable IOExceptions, and the sibling DataFile.record_count is bounded for this reason at hoglake_api_client.cpp:236) parses successfully; any call reaching GetStorageInfo (duckdb_tables(), DML planning on the table) then throws 'INTERNAL Error: Information loss on integer cast' and DuckDB invalidates the whole instance. Per this round's ground rule, a newly-found unguarded wire-value-to-InternalException path; also directly falsifies the R3 disposition's 'downstream NumericCasts on these fields are now provably in-range' claim.

**Verifier:** Confirmed end-to-end with no refuting guard. ParseTableInfo (src/rest/hoglake_api_client.cpp:176-178) parses Table-level record_count/file_count/file_size_bytes with unbounded GetInt, while sibling DataFile.record_count is bounded via GetBoundedInt at line 236 under the file's own stated invariant (lines 91-95: wire ints feeding NumericCast MUST be parse-bounded so malformed data is a catchable IOException). TryGetTable returns the struct verbatim; CacheTableInternal (hoglake_schema_entry.cpp:87-138) validates only column-name collisions; HoglakeTableEntry stores table_info unmodified. GetStorageInfo (hoglake_table_entry.cpp:107) then does NumericCast<idx_t>(record_count); the vendored numeric_utils.hpp:50 throws InternalException on negative int64→idx_t, and DuckDB invalidates the whole instance on ExceptionType::INTERNAL (per R3-1's verified client_context.cpp:368-370 chain). Reachable from duckdb_tables() (duckdb_tables.cpp:149) and INSERT binding (bind_insert.cpp:355). The R3 containment for unrepresentable tables only wraps fetch+construction — a negative value parses fine, so nothing fires there. The R3 disposition inventory literally lists DataFile/DeleteFile/Catalog/Snapshot/Column/PartitionField and omits Table, so its 'downstream NumericCasts provably in-range' claim is falsified exactly as the reviewer says. hoglake.yaml declares Table.record_count as bare int64 (no minimum), so no spec bound refutes it; a uint > INT64_MAX also reads negative via yyjson_get_sint, giving a second route. Not a documented-loud divergence; untouched by all three fix waves; no test covers it. High is correct per the round's ground rule (new unguarded wire-value→InternalException path = instance invalidation), same class as fixed HIGH finding R3-1.

## R4-4. [MEDIUM] (memory) src/storage/hoglake_table_entry.cpp:107

**Claim:** GetStorageInfo runs NumericCast<idx_t> on the table-level record_count that ParseTableInfo (src/rest/hoglake_api_client.cpp:176) parses with unbounded GetInt — the one wire integer feeding a NumericCast that the R3 GetBoundedInt sweep missed (TableInfo's record_count/file_count/file_size_bytes are absent from the sweep's DataFile/DeleteFile/Catalog/Snapshot/Column/PartitionField list) — so a negative value in a GET-table response throws InternalException and invalidates the whole instance.

**Failure scenario:** A GET /tables/{t} response carrying record_count: -1 (the OpenAPI Table schema declares plain int64 with no minimum; the server's hog_table_stats is additive-only today, so this needs a server-side bug or stats-row corruption, not a normal API sequence) parses cleanly, and then any SHOW ALL TABLES / duckdb_tables() call (duckdb_tables.cpp:149 calls table.GetStorageInfo) or INSERT ... ON CONFLICT bind (bind_insert.cpp:355) hits NumericCast's 'Information loss on integer cast' InternalException — every connection in the process fails FATAL 'database has been invalidated' until restart, the exact escalation class the R3 sweep claims is closed for all wire fields.

**Verifier:** CONFIRMED with no refuting guard on any layer. (1) Site: hoglake_table_entry.cpp:107 runs NumericCast<idx_t> on table_info.record_count (int64_t, hoglake_wire.hpp:70), parsed unbounded via GetInt at hoglake_api_client.cpp:176 — while sibling DataFile.record_count (line 236) is GetBoundedInt[0,MAX_I64], proving the sweep bounded DataFile but missed TableInfo's scalars; R3's wire-supplied inventory lists only DataFile/DeleteFile/Catalog/Snapshot/Column/PartitionField, exactly as claimed. (2) Guard sweep: GetInt passes any negative through; ParseTableInfo validates only decimal type_params; the R3-4 TryGetTable/listing containment catches only CatalogException/InvalidInputException at fetch+construction — record_count=-1 constructs a healthy entry and the cast fires later inside DuckDB core (duckdb_tables.cpp:149, bind_insert.cpp:355 — both verified in vendored source) with no catch; the only other consumer is hoglake_metadata_functions.cpp:207 (Value::BIGINT, safe). (3) Escalation: NumericCast-on-negative → InternalException → whole-instance invalidation was live-reproduced in this codebase in R3-3 with the identical mechanism. (4) Not documented-loud: it violates the code's own stated ground rule (hoglake_api_client.cpp:71-76 comment: wire integers feeding NumericCast MUST be parse-time bounded; R3 doc: "no wire-supplied value may reach an InternalException"). (5) Trigger realism matches the claim's honest framing: OpenAPI Table.record_count is plain int64 with no minimum, but the reference server cannot legally serve a negative (CommitService.kt:875 rejects negative per-file counts; hog_table_stats is additive-only per CommitService.kt:49 and CompactionService.kt:809, with DB CHECK backstops), so the trigger requires a server bug, stats-row corruption, or a non-reference/compromised endpoint — out-of-contract wire data, which is precisely the input class the client's hardening rule exists to convert into catchable IOExceptions. Severity medium is correctly calibrated: instance-wide blast radius, but out-of-contract trigger (vs R3-1's in-contract high).

## R4-5. [MEDIUM] (wire) src/rest/hoglake_api_client.cpp:133

**Claim:** The first layer of the two-layer decimal validation throws IOException (not CatalogException/InvalidInputException) for in-contract type_params the server accepts verbatim — precision/scale negative (GetBoundedInt range [0, 2^31-1]) or non-integer (JSON string/real) — and the widened R3-4 listing containment in hoglake_schema_entry.cpp (lines 168-173 and 207-212) catches only CatalogException and InvalidInputException, so the IOException escapes and breaks the whole catalog listing again.

**Failure scenario:** POST /tables with {"type":"decimal","type_params":{"precision":-1,"scale":0}} (or precision "10" / 3.5) succeeds server-side (Dto.kt:71 typeParams is Map<String,Any?> stored as jsonb with no validation — same acceptance R3-1/R3-4 verified live). The extension's SHOW ALL TABLES / duckdb_tables then dies with an uncaught 'hoglake: field "precision" ... out of range' IOException from LoadAllTablesInternal for the entire attached catalog, and the table is never remembered in unrepresentable_tables (order-dependent lookups regress too) — the exact catalog-introspection-dead class R3-4 was filed for, resurfacing through the round-3 parse-bounds seam because the belt CatalogException naming the table only fires for values inside the GetBoundedInt range.

**Verifier:** CONFIRMED by full-path static trace; every refutation avenue fails. (1) Site: hoglake_api_client.cpp:133-134 parses decimal type_params via GetBoundedInt(params, "precision"/"scale", 0, 2147483647); GetBoundedInt throws IOException for non-integer JSON (line 84) or out-of-range values (line 88), so precision -1, "10", or 3.5 all throw IOException at parse time — the belt CatalogException naming the table (lines 189-197) fires only for values inside GetBoundedInt's range (0 or 39..2^31-1). (2) No earlier guard: server CatalogService.createTable validates identifiers only; Dto.kt:71 typeParams is a freeform Map<String,Any?> stored as jsonb (TableRepo.kt:152) and echoed back verbatim (Dto.kt:103); no decimal validation anywhere in the server — the extension's own test fixture (read_fixture.py:242) already creates precision-100 poison tables via raw REST, proving acceptance of arbitrary type_params; precision -1 rides the same path. TryGetTable (api client 517-530) has no catch. (3) Containment gap: LoadAllTablesInternal (hoglake_schema_entry.cpp:207-212) and LookupTableInternal (:168-173) catch only CatalogException and InvalidInputException; IOException derives directly from Exception (vendored exception.hpp:234), a sibling class, so it escapes — Scan (:294) propagates it and SHOW ALL TABLES/duckdb_tables fails for the entire attached catalog; unrepresentable_tables is never populated, so the R3-12 remembered-error story regresses too. hoglake_table_info has the identical narrow catch pair (hoglake_metadata_functions.cpp:175-180). (4) Not a documented divergence — the code comment at schema_entry:194-199 explicitly claims the try covers parse-time wire validation of out-of-range decimal params (false for the IOException subset), and the R3 disposition marks R3-4 FIXED covering "both throw layers" while covering only the two exception types the existing fixtures produce; no test exercises negative/non-integer type_params. (5) Severity medium is correct per the R3-4 precedent: loud failure, no instance invalidation (IOException is ordinary and catchable), but catalog introspection is dead for the whole catalog until out-of-band repair.

## R4-6. [MEDIUM] (txn) src/storage/hoglake_delete.cpp:105

**Claim:** The round-3 duplicate-path DV fix registers each path's merged position set for EVERY live data_file_id of that path even though the scan's file_index column (delivered to the sink in row_id_indexes[1] and ignored at lines 36-54) distinguishes the logical copies, so a predicate matching rows of only ONE logical copy (rowid or _hog_snapshot_id predicates — the copies differ in both) silently deletes the same physical positions from ALL copies, committing removal of rows the predicate did not match; the UPDATE variant appends one rewritten row while masking the position in every copy, silently losing a row.

**Failure scenario:** Live-reproduced on the dev stack (scratch catalog duckext-sqltest, fixture-recreated afterward): ns1.dup_path reads rowids 0-5 over one physical 3-row parquet registered twice; DELETE FROM lakes.ns1.dup_path WHERE rowid = 3 succeeded (reported 1 row) but the post-commit table lost BOTH rowid 3 and rowid 0 — a committed row not matching the predicate vanished with no error and an undercounted report. Equivalently UPDATE ... SET a = 9 WHERE rowid = 3 appends one row of 9 while the embedded delete masks position 0 on both data_file_ids, net-losing one logical row. The R3 disposition's residual note covers only the count dedup, not this over-delete, and the in-code comment's 'must be registered for EVERY live data_file_id' is false — per-id attribution via file_index reproduces exact sequential semantics.

**Verifier:** Confirmed statically end-to-end with no refuting guard. The scan exposes duplicate-path logical files as separate file-list entries with distinct file_index, row_id_start-derived rowids, and snapshot_ids (hoglake_multi_file_list.cpp:191,256,258; hoglake_multi_file_reader.cpp:146-177), so rowid/snapshot_id predicates select rows of exactly one logical copy. The DELETE sink (hoglake_delete.cpp:36-54) reads only filename and file_row_number, ignoring the delivered file_index (row_id_indexes[1], wired at hoglake_catalog.cpp:181-184 and 204), keying positions by path alone; Finalize (line 105) then registers the merged position set for EVERY live data_file_id of that path. Result: DELETE WHERE rowid=3 on a twice-registered 3-row file masks physical position 0 on both data_file_ids, silently committing removal of rowid 0 (which did not match) while reporting 1 row; the UPDATE variant (same embedded sink) appends one rewritten row while masking the position in both copies, net-losing a row — HoglakeUpdate itself dedupes by (file_index,row_number), showing the distinguishing column reaches the sink boundary. Guard sweep: PlanDelete refuses only AT-pinned tables and RequireDMLAllowed shapes; nothing refuses duplicate-path tables (the R3 fix treats them as in-contract) or restricts predicates. The only test (hoglake_wire_hardening.test:174-187) uses a value predicate, the one shape where fan-out is correct, so the suite cannot detect this. Not documented-loud: the R3 residual note covers only count dedup, and the in-code comment's 'must register for every id' claim holds only for predicates matching both copies. Severity medium: silent wrong results on a committed DELETE/UPDATE, but requires the externally-created duplicate-path precondition plus a copy-distinguishing (rowid/snapshot_id) predicate.

## R4-7. [MEDIUM] (data) src/storage/hoglake_delete.cpp:54

**Claim:** The DELETE sink attributes positions by filename only (row_id_indexes[1] = file_index rides the delete chunk but is never read) and the duplicate-path fix then registers those positions for EVERY live data_file_id of the path, so a rowid predicate that matches a row in one logical copy silently deletes the same physical position from every other copy — rows that did not match the predicate are removed and committed, and the reported count (deduped by path+position) understates the rows that vanish.

**Failure scenario:** Live-reproduced on the dev stack (scratch catalog duckext-r4dc, table ns1.d = one 3-row parquet registered twice, row_id_start 0/3, reading {rowid 0..5}): DELETE FROM lake.ns1.d WHERE rowid = 4 reported 1 row but the post-commit table is {0,2,3,5} — rowid 1 (a=2, which did not match the predicate) is gone because position 1's DV was registered for both data_file_ids. UPDATE ... WHERE rowid = 4 has the same shape plus net row loss (2 rows deleted, 1 rewrite appended). The R3 dup-path disposition records only the delete-count dedup as residual; this cross-copy over-deletion is undocumented, and the wire-hardening test covers only value predicates (identical positions in every copy), so the suite cannot see it. Precondition (dup-path registration) is the same in-contract writer-retry state the R3 unverified finding established.

**Verifier:** CONFIRMED by static trace plus live reproduction. Code: hoglake_delete.cpp Sink (lines 36-54) keys deleted positions by the scan's filename column only — row_id_indexes[1] (file_index, wired by hoglake_catalog.cpp:181-185) is never read — and Finalize (lines 85-146, the R3 dup-path fix) registers the merged DV containing those physical positions for EVERY live data_file_id of the path. Since rowid = per-logical-file row_id_start + file_row_number (hoglake_multi_file_reader.cpp:146-177), a rowid predicate matches in exactly one logical copy but the position is deleted from all copies. Guard sweep found no earlier refusal: RequireDMLAllowed (hoglake_transaction.cpp:218-267) checks only own buffered appends and DDL interactions; PlanDelete refuses only pinned travel/RETURNING; the unrepresentable-table containment does not cover dup-path tables, and the R3 fix deliberately lets dup-path DML proceed. Not a documented divergence: R3's disposition records only the delete-count dedup as residual, DESIGN.md is silent, and hoglake_wire_hardening.test uses a value predicate whose matching positions are identical in every copy, so the suite cannot detect it. Live repro on scratch catalog duckext-r4verify (one 3-row parquet registered twice via the same in-contract writer-retry REST re-commit the R3 fixture uses): table reads rowids 0-5; DELETE FROM lake.ns1.d WHERE rowid = 4 commits and the post-commit table is {0,2,3,5} — rowid 1 (a=2), which did not match the predicate, was silently deleted, and the reported count (deduped by path+position) understates rows removed. UPDATE shares the same sink, adding net row loss. Severity medium is right: silent committed wrong results, gated on the externally-created but in-contract duplicate-path precondition — symmetric with R3's medium rating for the under-deletion half of the same state.

## R4-8. [MEDIUM] (data) src/rest/hoglake_api_client.cpp:176

**Claim:** ParseTableInfo parses record_count with unbounded GetInt — contradicting the R3 fix's parse-time-bounds ground rule (its GetBoundedInt coverage stops at DataFile/DeleteFile/Catalog/Snapshot/Column/PartitionField) — and the value reaches NumericCast<idx_t> in HoglakeTableEntry::GetStorageInfo (src/storage/hoglake_table_entry.cpp:107), which throws InternalException on any negative value and invalidates the whole instance.

**Failure scenario:** A server response carrying a negative record_count for any table (malformed proxy/older server; the current server derives it as a SQL sum of per-file counts validated non-negative, so this needs a non-conforming responder rather than an in-contract table) makes duckdb_tables() / SHOW ALL TABLES (duckdb_tables.cpp:149 calls GetStorageInfo per table) or any INSERT bind on that table (bind_insert.cpp:355) throw InternalException, after which every statement on the process fails FATAL 'database has been invalidated' until restart — the exact escalation class R3-1/R3-3 were filed for, sitting one unbounded field away from the completed sweep.

**Verifier:** Confirmed end-to-end. Parse: hoglake_api_client.cpp:176 uses unbounded GetInt for TableInfo.record_count (GetInt type-checks but has no range check), while the DataFile record_count at line 236 is bounded — and the R3 sweep's own coverage list (REVIEW-FINDINGS-R3.md:135-137) enumerates only DataFile/DeleteFile/Catalog/Snapshot/Column/PartitionField, so TableInfo's numerics were genuinely missed; the sweep's claim that downstream NumericCasts on wire fields are 'provably in-range' is false for this field. Sink: hoglake_table_entry.cpp:107 NumericCast<idx_t>(table_info.record_count); vendored numeric_utils.hpp:49-50 throws InternalException on negative→unsigned, which DuckDB escalates to whole-instance invalidation (live-established in R3-1/R3-3). Guard sweep found no client-side refusal anywhere on the path: TryGetTable→ParseTableInfo→entry construction stores the raw value; the R3-4 listing containment only catches throws during fetch/construction and nothing throws there since the field is unbounded; both sinks (duckdb_tables.cpp:149, bind_insert.cpp:355) call GetStorageInfo uncaught. Not a documented-loud divergence — the comment above GetBoundedInt (hoglake_api_client.cpp:71-75) states the opposite rule ('wire integers that feed NumericCast MUST be range-checked at parse time'). Reachability caveat verified and correctly priced into the claimed severity: the current server cannot emit a negative (per-file counts validated ≥0 at CommitService.kt:875; hog_table_stats only increments validated sums; deletes/compaction leave it untouched), so unlike R3-1 this is not plantable via raw REST — it needs a non-conforming responder/proxy. That keeps it medium rather than high, but it squarely violates the project's own R3 wire-hardening contract in the instance-invalidation class.

## R4-9. [MEDIUM] (catalog) src/storage/hoglake_schema_entry.cpp:207

**Claim:** The widened unrepresentable-table containment catches only CatalogException and InvalidInputException, but the round-3 parse-time wire validation it wraps throws IOException (GetBoundedInt, hoglake_api_client.cpp:88, and the GetString/GetInt type checks), which is a sibling class and escapes — one poison table breaks every catalog listing again, the exact R3-4/R2-4 regression class the fix disposition claims closed.

**Failure scenario:** POST /tables with column {"type":"decimal","type_params":{"precision":5000000000}} (or precision -1, or precision as a JSON string) succeeds server-side — R3-1 already proved type_params are persisted unvalidated (additionalProperties:true, precision 300 accepted live). ParseColumn's GetBoundedInt(params, "precision", 0, 2147483647) then throws IOException 'field precision out of range', which neither catch at hoglake_schema_entry.cpp:207/210 handles: SHOW ALL TABLES / duckdb_tables() / any Scan of the attached catalog fails wholesale (all_tables_loaded never set, nothing remembered in unrepresentable_tables), and hoglake_table_info dies the same way (hoglake_metadata_functions.cpp:175-181 has the identical two-catch gap). The fixture's poison tables (bad_dec100, bad_dec_nop) only exercise the in-int32-range CatalogException belt and the missing-params InvalidInputException, so the suite cannot see it. Catalog introspection is dead for every DuckDB client until out-of-band repair; direct-name lookup of healthy tables still works.

**Verifier:** Confirmed end-to-end. GetBoundedInt (src/rest/hoglake_api_client.cpp:77-91) throws IOException for non-integer or out-of-[0,2147483647] precision, thrown from ParseColumn (:133) BEFORE ParseTableInfo's CatalogException belt (:185-197), which only covers values that survived the bound check (e.g. precision 100/300) — the two validation layers are disjoint by exception type. The listing containment at hoglake_schema_entry.cpp:207/210 (and the cold-lookup catches at :168/:171, and TableInfoInit in src/functions/hoglake_metadata_functions.cpp) catches only CatalogException and InvalidInputException; IOException derives directly from Exception in the vendored DuckDB (exception.hpp:234) and escapes, so all_tables_loaded is never set and nothing is remembered in unrepresentable_tables — every SHOW ALL TABLES / duckdb_tables / hoglake_table_info fails wholesale. Reachability has no earlier guard: server typeParams is Map<String, Any?> (api/Dto.kt:71) and CatalogService.createTable (service/CatalogService.kt:168-208) validates only identifiers/non-empty/duplicate columns, so precision -1, 5000000000, or a JSON string persists and is echoed (R3-1 live-proved this persistence path; none of the three fix waves touched the server). The fixture's poison tables (read_fixture.py: bad_dec100=precision 100, bad_dec_nop=missing params) exercise only the two caught exception types, so hoglake_wire_hardening.test cannot see this. Not a documented-loud divergence — the comment above the try explicitly promises parse-time wire validation is covered and the listing never breaks. No instance invalidation (IOException is not escalated), healthy tables remain reachable by direct name, trigger requires an out-of-band client: medium is the right severity, matching the R3-4 class.

## R4-10. [MEDIUM] (tests) test/sql/hoglake_compacted_read.test:43

**Claim:** The PARITY.md:68 rowid DONE upgrade and this test's own comment ('asserting values + count catches a positional-fallback regression') exceed proof: the fixture's compaction output has row_id_start=0 with an order-preserving merge (live wire shows row_id_start 0, 5 records; ParquetRewriter writes _hog_row_id = rowIdStart+ordinal, CompactionService registers minRowId=0), so the asserted rowids 0..4 are byte-identical to what positional row_id_start+file_row_number arithmetic produces.

**Failure scenario:** Reverting the whole explicit-row-id feature (the TryFindColumnByFieldId branch at hoglake_multi_file_reader.cpp:150 plus the row_id_start omission at hoglake_multi_file_list.cpp:255) leaves the entire suite green — positional fallback yields the same 0..4 — so a regression to positional rowids on compaction outputs ships undetected and silently mis-reports rowids on any compacted file whose ids are non-contiguous or non-zero-based (any table with pre-compaction deletes); a discriminating fixture needs exactly that shape (e.g. delete one points row before force-compacting).

**Verifier:** Every factual premise verified against the live stack and code. Fixture (test/fixtures/read_fixture.py:90-95,312-336) creates points fresh with no deletes, so the compaction merge preserves order; live /scan shows the single compacted file with row_id_start=0, explicit_row_ids=true, 5 records; direct parquet read from MinIO confirms _hog_row_id == file_row_number (0..4) with physical order id 1..5 — so positional row_id_start+file_row_number arithmetic is byte-identical to the explicit column. Reverting both the TryFindColumnByFieldId branch (hoglake_multi_file_reader.cpp:150) and the row_id_start omission guard (hoglake_multi_file_list.cpp:255) leaves hoglake_compacted_read.test and the whole suite green: no other test touches an explicit-row-id file. The test comment (lines 38-42) and PARITY.md:68 overclaim that the assertion catches a positional-fallback regression, when the fixture satisfies exactly the coincidence condition the comment itself names (order-preserving AND starting at 0). Partial mitigation exists — the row_id_start omission means a broken reader branch alone fails loudly via InvalidInputException at hoglake_multi_file_reader.cpp:159 — so the test does prove the branch executes; but it cannot detect a full-feature revert, whose masked failure is silent wrong rowids on any compacted file with non-contiguous ids (and rowid-driven DELETE/UPDATE would then target wrong rows). A discriminating fixture (delete one row before force-compact) would work as the reviewer describes. Medium severity is right for a coverage-gap/overclaim finding.

## R4-11. [LOW] (tests) src/storage/hoglake_schema_entry.cpp:27

**Claim:** The R3-5 disposition's 'the false "the server 422s them" comment is corrected' is itself false: the file-header comment for RESERVED_COLUMN_PREFIX still asserts 'the server 422s them', which the R3-5 verifier proved wrong (server name validation is pattern-only and accepts _hog_row_id); only the ALTER-site comment at lines 596-604 was corrected.

**Failure scenario:** A future fix wave reading the header comment concludes the client-side _hog checks are redundant belt-and-braces over server enforcement and weakens them, reopening the R3-5 path (user _hog_row_id columns committed into the shared catalog, pyhoglake appends broken, compaction emitting duplicate _hog_row_id fields) — the same stale-contract-comment class as R3-10.

**Verifier:** Verified in full. (1) hoglake_schema_entry.cpp:27 still asserts "the server 422s them" — the sole surviving instance of that false claim in the extension (grep-confirmed). (2) The falsity is independently confirmed: server Identifiers.kt validates names against the pattern ^[A-Za-z_][A-Za-z0-9_-]{0,127}$ only (accepts _hog_row_id), and no reserved-prefix enforcement exists anywhere in the server tree; bf7de4c's own commit message says server enforcement is merely a "server finding filed". (3) git show bf7de4c confirms the fix wave added only the ALTER-site checks with corrected comments (lines 571-576, 597-601, which correctly say "the server does NOT enforce the _hog reservation... the client is the enforcement point") and never touched line 27 — the file now contradicts itself. (4) REVIEW-FINDINGS-R3.md:159's disposition 'the false "the server 422s them" comment is corrected' is therefore inaccurate as to the header comment, exactly as claimed. (5) The stale-contract-comment class has precedent in this series: R3-10 (stale DESIGN.md sentence) was accepted at LOW and fixed. The regression scenario is plausible and concrete: the header falsely presents the client check as belt-and-braces over server enforcement, inviting a future weakening of the only enforcement point, which would reopen the live-reproduced R3-5 path (committed user _hog_row_id column, pyhoglake appends broken cross-client, ParquetRewriter emitting duplicate _hog_row_id fields with name-based srcRowIdIndex corruption on recompaction). Severity low is correct: documentation/disposition-record defect, no runtime path today.


---

# Round-4 fix dispositions (2026-09-14, this branch)

All 11 confirmed findings fixed. Suite: `./test/run-live-tests.sh`
green — 11 sqllogictest files / 630 assertions + cross-client wire
check.

## Corrected sweep rule, re-run

The R3 classification rule was wrong exactly as stated: a check is
engine-internal only when BOTH sides are engine-derived. Re-swept
every comparison and cast under that rule:

- The delete sink's position-bounds check (positions from scan
  ordinals + object-store DVs + buffered sets, bound from wire
  record_count) → typed InvalidInputException naming table, file,
  position, and the offending registration's data_file_id and
  declared row count (R4-1/R4-2).
- ParseTableInfo's Table-schema numerics (the struct the R3 sweep
  silently omitted): record_count/file_count/file_size_bytes now
  GetBoundedInt [0, MAX_I64]; ParseCommitResult bounded the same way.
  GetStorageInfo's NumericCast is now provably in-range (R4-3/R4-4/
  R4-8).
- HoglakePuffin::EncodeBlob's uint32 length cast can see object-store
  DV contents through the union → typed size refusal before the cast.
- Remaining sites re-verified: both-sides-engine only (scan
  file_row_number vs its own set; copy-stats idx_t→int64; puffin
  writer internals; FileHandle::GetFileSize already idx_t).

| # | Disposition |
|---|---|
| R4-1 (HIGH) | FIXED. Typed refusal (see sweep); DELETE on the fixture's `dup_incon` (3-row parquet registered twice, second declaring 2 rows — server accepts) errors with the targeted message, nothing commits, instance survives — asserted with a liveness query (`hoglake_wire_hardening.test`). Policy per the mandate: refuse loudly, never clamp — documented in DESIGN.md "Write path". |
| R4-2 (HIGH) | FIXED by the same guarded line: the committed-DV union flows into the identical typed check. An out-of-range DV from another client now refuses with the same per-table error naming file and position. (No separate live construction: hoglake has no client DV-writer to plant one with; the R4-1 repro exercises the same throw site with the same union inputs.) |
| R4-3 (HIGH) / R4-4 / R4-8 (MED) | FIXED (see sweep): the omitted Table schema is bounded at parse; the R3 "provably in-range" claim is now actually true and the disposition inventory above names the omission. |
| R4-5 / R4-9 (MED) | FIXED by unifying the exception taxonomy (documented at the top of hoglake_api_client.cpp): IOException = transport/protocol (PROPAGATES — a down server must never fake an empty catalog); InvalidInputException = malformed wire DATA (containable); CatalogException = object-scoped wire data error (containable). GetBoundedInt and field-level type mismatches now throw InvalidInputException, so the existing containment pair (CatalogException + InvalidInputException) covers every data-shaped throw — no catch-set widening needed, and transport failures still fail loud. Layer-1 decimal (parse bound) and layer-2 (named-table CatalogException) are both containable, unifying the R4-5 complaint. Test: fixture table `bad_dec_neg` (precision −1, the parse-layer throw): targeted error on lookup, listings and hoglake_table_info survive, instance alive. |
| R4-6 / R4-7 (MED) | FIXED. Positions are attributed per LOGICAL registration via the rowid base (rowid − file_row_number == row_id_start for positional files): single-registration paths behave as before (explicit-row-id files included — varying bases union harmlessly); duplicate-registered paths attribute exactly, and ambiguous shapes (an explicit-row-id duplicate, two registrations sharing row_id_start, an unmatched base) refuse typed. file_index was evaluated and rejected as the attribution key: it indexes the SCAN's (possibly partition-pruned) file list, whose indices need not map to the finalize-time plan — the rowid base is stable under pruning. The UPDATE path ships the rowid through its delete chunk. Live+suite verified: DELETE WHERE rowid = 4 on `dup_path` removes only the matching copy's row (the other copy's a=2 survives); value-predicate deletes still remove every copy (`hoglake_wire_hardening.test`, rerun-stable). This also fixes the R3 residual: reported counts are now per logical row, not per (path, position). |
| Duplicate-target 422 reconciliation | VERIFIED COMPATIBLE, no change needed: the server's validateDeleteRegistrations rejects duplicate data_file_id targets within one commit (CommitService.kt:955-958) and is silent on duplicate PATHS across distinct ids; the client emits exactly one registration per data_file_id (AddDeletes replaces by id), and the dup-path commits have passed live since R3 (re-verified this round through the dup_path/dup_incon tests). |
| R4-10 (MED) | FIXED. Fixture adds `points_sorted`: 8 single-row batches with a DESCENDING key, compacted under an ASCENDING sort order — the sorted merge permutes _hog_row_id against physical position, so positional-fallback arithmetic yields different rowids. Gated test (`DUCKEXT_SORTED_COMPACTED`) asserts the explicit pairs; PARITY.md re-worded to claim exactly this. (The tiered planner stalls above one file for small inputs; the gate accepts any explicit output, which suffices — each sorted output is internally permuted.) |
| R4-11 (LOW) | FIXED. The stale header comment now states the truth: the server does NOT enforce the _hog reservation (finding 9), this client is the enforcement point. |

Also hardened while in the area (same corrected-rule spirit, wire
strings feeding casts): hoglake_snapshots' snapshot_time and
hoglake_table_info's uuid wire strings now TryCast per row (malformed
value → NULL cell, never a dead listing).
