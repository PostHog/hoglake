# docker-java negotiates no API version and pins 1.32 when unset; recent
# Docker daemons (OrbStack's Docker 29+) reject clients below 1.40, which
# breaks Testcontainers discovery ("Could not find a valid Docker
# environment"). Project-scoped override; remove when the bundled
# docker-java negotiates properly.
api.version=1.44
